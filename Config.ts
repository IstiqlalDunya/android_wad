const Config = {
  settings: {
    // 10.0.2.2 is the Android emulator's alias for the host machine's localhost
    serverPath: 'http://10.0.2.2:5000',
    wsPath: 'http://10.0.2.2:5001',
  },
};

export default Config;